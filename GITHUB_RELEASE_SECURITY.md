# GitHub 发布前安全处理

仓库只发布源码和完全虚构的演示材料。`data/`、上传件、数据库、账号文件、运行日志和真实环境配置不得提交。

如果令牌曾进入远端历史，必须按以下顺序处理：

1. 立即在中转服务或云平台撤销旧令牌并生成新令牌；仅删除 Git 内容不能让旧令牌失效。
2. 在一份全新克隆中使用 `git filter-repo` 删除数据库、上传目录和敏感配置；不要在仍含个人资料的工作目录直接操作。
3. 运行 `python scripts/github_release_check.py`，确认当前索引和全部本地历史均通过。
4. 通知协作者暂停推送，再对清理后的分支和标签执行受保护的强制更新；所有协作者必须重新克隆。
5. 在 GitHub 安全设置中开启 Secret scanning、Push protection 和 Dependabot alerts。

本项目目录当前没有配置远端时，发布检查只能验证本地可见的索引和提交，不能证明另一个 GitHub 仓库的历史已经清理。远端令牌撤销状态也必须在云平台控制台单独核验。
