# 知识中心改造记录（2026-08-22，已于 2026-08-23 完成）

> 状态：候选自动刷新与语义结果同步、知识树多选拖动、PPT 自适应预览均已实现并通过测试；本文件保留为实施记录。

## 本轮需求

1. 语义分析完成后，新的待审核知识点立即显示在“知识点候选 → 知识点队列”。
2. “知识结构治理”的目录树像文件夹一样支持多选、拖动、拆分和合并。
3. PPT 网页预览根据容器自适应缩放，完整显示幻灯片。

## 已定位

- 后端实际已有 26 个新的 `PENDING` PPT 候选；页面未更新是因为任务变为 `review_required` 时，轮询只调用 `loadOutline()`，没有重新调用 `loadIngestionMetadata()`。
- 当前树虽有 checkbox，但未启用 `draggable / allow-drag / allow-drop / node-drop`，后端也没有批量移动节点接口。
- PPT 渲染写死为 `960 × 540`，而预览列通常约 420px，导致只能看到半张。
- 旧 PPT 的任务页数可能小于 `presentation_slides` 的实际幻灯片数，预览描述应返回真实页数。

## 已完成实施

- 分析完成时同时刷新知识树和候选数据；用最新语义 `points + source blocks` 重建尚未审核的候选，保留教师已经批准、修改或驳回的决定。
- 新增批量移动接口，严格限制“章 → 根、节 → 章、知识点 → 节”；前端拖动已勾选的同类型节点时整组移动。
- PPT 按容器宽度渲染并保持比例，使用 `ResizeObserver` 响应窗口变化；切换审核栏目后重新挂载预览。
- PPT 预览描述返回 `presentation_slides` 的真实数量。

## 完成状态与验证

- 本轮补丁已应用；之前完成的 `semantic_role`、旧断点、PPT 标题和 API/Worker 指纹修复均保留。
- API、Worker、Vue 已重启，API/Worker 源码指纹均为当前版本。
- 后端相关回归测试 23 项通过；前端 2 项通过；`npm run build` 成功。
